./gradlew tasks

./gradlew compileJava --build-cache


org.gradle.console=verbose
org.gradle.caching=true


To permanently enable verbose mode, add org.gradle.console=verbose to your gradle.properties file.

Some builds may contain a build.gradle(.kts) file in the root project but this is NOT recommended.

You can also list the tasks only available in the app subproject by running ./gradlew :app:tasks.

You can obtain more information in the task listing using the --all option: ./gradlew tasks --all.

./gradlew :app:dependencies
./gradlew projects


https://hub.docker.com/r/gradle/build-cache-node

Kotlin DSL: https://docs.gradle.org/current/kotlin-dsl/index.html
Gradle API: https://docs.gradle.org/current/javadoc/?_gl=1*1fdh00u*_gcl_au*MjUwNTIyMjYzLjE3Mjc4NzE4Njg.*_ga*NzI3MTMyMDI5LjE3Mjc4NzE4Njg.*_ga_7W7NC6YNPT*MTczMjk2NTU4MC4xNS4xLjE3MzI5Njg0NzcuNTkuMC4w


3 Phases"
1. Initialization. Evaluates settings.gradle.kts. Creates Settings and Project for every project
2. Configuration. Evaluates all builds scripts build.gradle.kts. Creates tasks graph.
3. Execution. Schedules and executes the selected tasks.

settings.gradle.kts:
 rootProject.name = "basic"
 println("This is executed during the initialization phase.")

build.gradle.kts:
  println("This is executed during the configuration phase.")

  tasks.register("test") {
      doLast {
          println("This is executed during the execution phase.")
      }
      println("This is executed during the configuration phase, only when the "test" task is requested.")
  }

It’s important to remember that while many Gradle scripts are typically written in short Groovy or Kotlin syntax,
every item in the settings script is essentially invoking a method on the Settings object in the Gradle API:



>>> Settings:
API: https://docs.gradle.org/current/javadoc/org/gradle/api/initialization/Settings.html
DSL: https://docs.gradle.org/current/dsl/org.gradle.api.initialization.Settings.html

>>> Project:
API: https://docs.gradle.org/current/javadoc/org/gradle/api/Project.html
DSL: https://docs.gradle.org/current/dsl/org.gradle.api.Project.html

